{
  "name": "app-notifier",
  "version": "1.0.0",
  "description": "App client notifier built with Electron.js",
  "main": "index.js",
  "scripts": {
    "start": "electron .",
    "dev": "electron . --dev",
    "build": "electron-builder",
    "dist": "electron-builder",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "koki198977",
  "license": "ISC",
  "devDependencies": {
    "electron": "^28.0.0",
    "electron-builder": "^24.9.1"
  },
  "build": {
    "appId": "com.notifier.realdev",
    "win": {
      "target": "nsis"
    }
  }
}
