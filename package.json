{
  "name": "server",
  "version": "1.0.0",
  "description": "app client notifier electronjs.io",
  "main": "index.js",
  "scripts": {
    "electron": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
  },
  "devDependencies": {
    "electron": "^11.2.3"
  }
}
